# Handoff: S3 cutover

**Status:** Client + Laravel pairing are S3-only. MinIO live at `s3.rui.cam`. Forge env + Windows build + re-pair still gate production.

## Three systems

| System | Where | Job |
| --- | --- | --- |
| Control plane | `box-rui-cam` → `backup.rui.cam` | Pair / approve / issue S3 creds |
| Sync app | this repo (`s3-multipart-implementation`) | Win7 client |
| Object storage | Proxmox MinIO CT 101 → `s3.rui.cam` | Bytes |

```text
Client --pair--> backup.rui.cam
Client --S3---> s3.rui.cam
```

Authority: `SPEC.md` / `AGENTS.md` (client) · `BACKUP_SYNC_COMMUNICATION_SPEC.md` (Laravel).

## Done

- Client: `BackupTransport` + S3 PutObject/multipart; `pair_api_base` = `https://backup.rui.cam`.
- Laravel: S3 approve payload; `S3_BACKUP_PUBLIC_ENDPOINT` default `https://s3.rui.cam`.
- MinIO: public TLS (Let's Encrypt), health 200 on `s3.rui.cam`.

## Open (operator)

1. Forge on **backup.rui.cam**: paste `S3_BACKUP_*` from Proxmox `/root/s3-minio-creds.txt`, deploy `s3-multipart-implementation`, `config:clear`.
2. Win10 VM 102: `git pull` + `.\build-local.ps1` (Win7 target).
3. Re-pair devices still on old WebDAV config.
4. Admin S3 scanner / provider-key revoke (later).

## Build

```powershell
.\build-local.ps1   # x86_64-win7-windows-msvc only
```

See `proxmox/win10-build-vm.md`.
