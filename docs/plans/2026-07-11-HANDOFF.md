# Handoff: S3 cutover

**Status (2026-07-12):** Code + MinIO public TLS are done. Production cutover is **not** finished — operator Forge env/deploy, Windows build, re-pair, and smoke upload still open.

**Agents:** never access Forge (no deploy, tokens, or production `.env`).

## Three systems

| System | Where | Job |
| --- | --- | --- |
| Control plane | `box-rui-cam` → `backup.rui.cam` | Pair / approve / issue S3 creds |
| Sync app | this repo (`s3-multipart-implementation`) | Win7 client |
| Object storage | Proxmox MinIO CT 101 → `s3.rui.cam` | Bytes |

```text
Client --pair--> backup.rui.cam
Client --S3---> s3.rui.cam
```

Do not conflate pairing URL with object storage. Legacy `box.rui.cam` = old WebDAV host.

Authority: `SPEC.md` / `AGENTS.md` (client) · `BACKUP_SYNC_COMMUNICATION_SPEC.md` (Laravel) · `proxmox/s3-minio-ct.md` (MinIO).

## Done (verified)

- Client: `BackupTransport` + S3 PutObject/multipart; `pair_api_base` = `https://backup.rui.cam`.
- Laravel branch: S3 approve payload; approve folder list via S3 (not WebDAV); defaults `S3_BACKUP_PUBLIC_ENDPOINT=https://s3.rui.cam`.
- MinIO: public TLS (Let's Encrypt), `https://s3.rui.cam/minio/health/live` → **200**.
- Edge: router **:443** → Proxmox host Caddy → CT `:9000`. No need to open `:9000`/`:9001` publicly. Certs are **not** the blocker.

## Open (operator / build)

1. **Forge on backup.rui.cam (human only):** `S3_BACKUP_*` with `https://s3.rui.cam`, deploy Laravel branch with auto-provision, `php artisan migrate --force`, `config:clear`. Host needs PHP `ext-ffi` + `libargon2-1`.
2. **Smoke:** Pair → Approve (no Artisan) → client `transport: s3` + `s3_endpoint: https://s3.rui.cam` → upload under `customer/uuid/`.
3. **Revoke smoke:** Revoke device → MinIO key dead → re-pair works.
4. **Win10 VM 102:** `git pull` + `.\build-local.ps1`.
5. **Re-pair** old WebDAV devices.
6. Later: admin S3 scanner.

## Docs that were wrong (fixed)

- Object store is **MinIO**, not Garage.
- Client `s3_endpoint` is **`s3.rui.cam`**, not `backup.rui.cam`.
- “Almost done” meant code + MinIO edge — not live cutover.

## Build

```powershell
.\build-local.ps1   # x86_64-win7-windows-msvc only
```

See `proxmox/win10-build-vm.md`.
