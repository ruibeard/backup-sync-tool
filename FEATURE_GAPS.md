# Feature Gaps

This file lists checklist items that are still missing or intentionally deferred.

## Remaining Gaps

- [ ] None currently identified from the confirmed checklist

## Planned Later

- [ ] Expand `Open Logs` beyond opening the logs folder if you later want a richer logs experience
- [ ] Add a visible progress bar if you want more than the compact activity feed and syncing animation

## Confirmed Decisions

- [x] Keep the direct XD DLL/license detection approach
- [x] Do not require a visible `Close` button in the current UI
- [x] Use `Backup Sync Tool` naming instead of old `WebDavSync` branding
- [x] Recent Activity is a compact transfer feed, not a timestamped log viewer
- [x] Parallel uploads are intentionally bounded to a small fixed limit to avoid excessive memory use
- [x] Tray sync indication uses tooltip text plus animated icon instead of a custom numeric tray overlay
- [x] Recent Activity is intentionally minimal and shows only transfer direction plus filename
