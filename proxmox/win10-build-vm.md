# Win build notes (optional lab)

Windows builds are **local**: on any machine with Rust nightly + VS Build Tools run `.\build-windows.ps1`.

No Proxmox / remote host is required. Older VM lab notes are obsolete for the app build path.
